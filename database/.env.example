# Copy this file to a location outside source control and set a real password.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=cloud_security
DB_USER=cloud_app
DB_PASSWORD=replace-with-a-secret
# Keep false for managed MySQL / production. true is local-development only.
DB_SSL_DISABLED=false
